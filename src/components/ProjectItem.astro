---
export interface Props {
  name: string;
  description: string;
  imageUrl: string;
  className: string;
}

const { name, description, imageUrl, className } = Astro.props;
---

<div class={`card ${className}`}>
  <img src={imageUrl} alt={name} />
  <div class="text-container">
    <h3>{name}</h3>
    <p>{description}</p>
  </div>
</div>

<style lang="scss">
  .card {
    width: 100%;
    height: 480px;
    background-color: var(--lightest-peach);
    border-radius: 16px;

    @media screen and (max-width: 768px) {
      display: flex;
      align-items: center;
    }

    img {
      width: 100%;
      height: 320px;
      object-fit: cover;
      border-radius: 16px 16px 0 0;

      @media screen and (max-width: 768px) {
        width: 50%;
        height: 100%;
        border-radius: 16px 0 0 16px;
      }
    }

    .text-container {
      padding: 32px;
      display: flex;
      flex-direction: column;
      gap: 8px;
      text-align: center;

      h3 {
        color: var(--peach);
        text-transform: uppercase;
        letter-spacing: 5px;
      }

      p {
        color: var(--dark-grey);
        font-size: 16px;
        line-height: 26px;
      }
    }
  }
</style>
